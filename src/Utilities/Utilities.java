package Utilities;

public class Utilities {

    /** an enumeration containing steering commands */
    public enum Direction {UP, DOWN, LEFT, RIGHT, NONE};
    
    /** an enumeration containing itemtypes */
    public enum ItemType { FOOD, WALL, WORM };
}
